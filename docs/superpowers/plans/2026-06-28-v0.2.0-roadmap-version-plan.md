# v0.2.0 Roadmap and Version Checkpoint Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mark the current OS inventory and network tools work as the v0.2.0 checkpoint and establish a public roadmap for sequential version upgrades.

**Architecture:** Keep version metadata in `package.json` and `src/core/version.ts` synchronized. Move completed user-visible work into a dated `CHANGELOG.md` v0.2.0 section and add `ROADMAP.md` as the source of truth for upcoming version milestones. Keep verification through the existing Bun harness.

**Tech Stack:** Bun, TypeScript, Biome, Markdown, `bun run verify`.

---

## Task 1: Version Metadata

**Files:**
- Modify: `package.json`
- Modify: `src/core/version.ts`

- [ ] **Step 1: Bump package version**

Change `package.json`:

```json
"version": "0.2.0"
```

- [ ] **Step 2: Bump runtime version constant**

Change `src/core/version.ts`:

```ts
export const VERSION = "0.2.0";
```

- [ ] **Step 3: Verify version command**

Run:

```bash
bun src/bin/picos.ts version
```

Expected output:

```txt
0.2.0
```

## Task 2: Public Roadmap

**Files:**
- Create: `ROADMAP.md`
- Modify: `README.md`

- [ ] **Step 1: Create roadmap document**

Create `ROADMAP.md` with:

```md
# picos Roadmap

## v0.2.0 - OS Inventory and Network Tools

Status: complete on `codex/picos-v0.1-scaffold`.

- Full-screen terminal OS shell.
- System, hardware, storage, process, network, action, status, and log panels.
- `picos info --full`.
- Safe `ping` options.
- Safe TCP connect check via `picos connect <host> <port>`.
- Read-only action model with write/destructive actions locked.
- Agent docs and verification harness.

## v0.2.1 - Console Polish

Goal: make the current TUI feel denser, cleaner, and more OS-like.

- Improve panel spacing and truncation.
- Add richer status badges for permission, network, and command state.
- Add clearer Network Tools action previews.
- Improve small-terminal sidebar window behavior.
- Keep all write/destructive actions locked.

## v0.3.0 - DOS/File Manager

Goal: add the first local filesystem console.

- Files workspace.
- Safe read-only commands: `pwd`, `dir`, `ls`, `cd`, `type`, `cat`.
- Text/Markdown viewer.
- Draft text editor behind explicit save confirmation.
- Delete/move/copy remain locked until preview and confirmation are implemented.

## v0.4.0 - Privileged Controls Framework

Goal: prepare real OS mutation without making it casual or dangerous.

- Preview/dry-run framework.
- Confirmation phrase flow.
- Admin/elevation detection by platform.
- Audit log for write/destructive attempts.
- First controlled mutation prototype.

## v0.5.0 - Developer Environment Plugins

Goal: expand beyond local OS inventory into developer operations.

- Plugin registry design.
- Docker read-only plugin.
- SSH profile inventory.
- Logs workspace.
- System monitor workspace.
```

- [ ] **Step 2: Link roadmap from README**

Add a Roadmap section to `README.md`:

```md
## Roadmap

See [ROADMAP.md](ROADMAP.md) for the sequential version plan.
```

## Task 3: Changelog Release Section

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Add v0.2.0 release heading**

Change:

```md
## [Unreleased]

### Added
```

to:

```md
## [Unreleased]

### Added

## [0.2.0] - 2026-06-28

### Added
```

- [ ] **Step 2: Keep security notes under v0.2.0**

Leave the existing `### Security` section under `## [0.2.0] - 2026-06-28`.

## Task 4: Verification and Commit

**Files:**
- All touched files.

- [ ] **Step 1: Run formatter/checks**

Run:

```bash
bunx biome check . --write
bun run verify
```

Expected: PASS with all tests, typecheck, build, and smoke checks complete.

- [ ] **Step 2: Commit checkpoint**

Run:

```bash
git add .
git commit -m "chore: mark v0.2.0 roadmap checkpoint"
```

- [ ] **Step 3: Push checkpoint**

Run:

```bash
git push
```

Expected: `codex/picos-v0.1-scaffold` pushed to origin.
